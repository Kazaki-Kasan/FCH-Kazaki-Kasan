<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Compound Interest Calculator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/CIC.css" />
</head>

<%- include('partials/navbar') %>

<body>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h2>Compound Interest Calculator</h2>
      <button id="currencyToggle" class="btn btn-outline-secondary">Switch to ₹</button>
    </div>

    <ul class="nav nav-tabs mb-3" id="calcTabs" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="compound-tab" data-bs-toggle="tab" data-bs-target="#compound" type="button" role="tab">Compound Interest</button>
      </li>
    </ul>

    <div class="tab-content" id="calcTabsContent">
      <div class="tab-pane fade show active" id="compound" role="tabpanel">
        <form id="compoundForm">
          <div class="mb-3">
            <label for="principal" class="form-label">Principal Amount</label>
            <input type="number" class="form-control" id="principal" required />
          </div>
          <div class="mb-3">
            <label for="rate" class="form-label">Annual Interest Rate (%)</label>
            <input type="number" class="form-control" id="rate" required />
          </div>
          <div class="mb-3">
            <label for="years" class="form-label">Time (Years)</label>
            <input type="number" class="form-control" id="years" required />
          </div>
          <div class="mb-3">
            <label for="frequency" class="form-label">Compounding Frequency</label>
            <select class="form-select" id="frequency">
              <option value="1">Annually</option>
              <option value="2">Semiannually</option>
              <option value="4">Quarterly</option>
              <option value="12" selected>Monthly</option>
            </select>
          </div>
          <button type="submit" class="btn btn-primary">Calculate</button>
        </form>

        <div class="mt-4">
          <p><strong>Final Amount:</strong> <span id="finalAmount">$0</span></p>
          <p><strong>Total Interest Earned:</strong> <span id="interestEarned">$0</span></p>
        </div>

        <canvas id="compoundChart" class="mt-4" height="100"></canvas>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="/js/CIC.js"></script>
<%- include('partials/footer') %>
</body>
</html>
